<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minhas Candidaturas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            color: #343a40;
            font-family: 'Arial', sans-serif;
        }

        .container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            text-align: center;
        }

        .btn-placeholder {
            margin-top: 20px;
        }
    </style>
</head>

<body>

    <div class="container">
        <h2>Minhas Candidaturas</h2>

        <!-- Verifique se o usuário tem candidaturas -->
        <div id="candidatura-info">
            <p>Você tem um total de <span id="candidatura-count">0</span> candidaturas.</p>

            <div id="no-candidaturas" style="display: none;">
                <h4>Você ainda não se candidatou a nenhuma vaga...</h4>
                <p>Atualize o seu currículo e comece a se candidatar a novas vagas.</p>
                <button class="btn btn-primary btn-placeholder" onclick="window.location.href='#'">Achar vagas</button>
            </div>
        </div>
    </div>

    <script>
        // Simule o número de candidaturas
        let candidaturas = 0; // Altere este valor para testar

        // Atualize a contagem de candidaturas
        document.getElementById('candidatura-count').innerText = candidaturas;

        // Mostre a mensagem se não houver candidaturas
        if (candidaturas === 0) {
            document.getElementById('no-candidaturas').style.display = 'block';
        }
    </script>

</body>

</html>
